import 'package:flutter/material.dart';

class AppConstants {
  static BorderRadius borderRadius = BorderRadius.circular(10);
  static const pagePadding = EdgeInsets.all(8);
  static const contentPadding =
      EdgeInsets.symmetric(vertical: 20, horizontal: 20);
  static const widthSizedBox = SizedBox(
    width: 10,
  );
  static const heightSizedBox = SizedBox(
    height: 10,
  );

  static const firstRowText = "Contains at least 8 characters";
  static const secondRowText = "Contains at least 1 number";
}


